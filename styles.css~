:root{
    --page-background: #DFD7C2;
    --calc-background:#56544E ;
    --display-background:#DFD7C2;
    --display-numb-color:black;
    --num-button:#9AA27C;
    --func-button:#69392E;
    --animation-color:#CA8C49;
     }
html{
    box-sizing:border-box;
}
body{
    background-color:var(--page-background);
}
/*------------------------------------------*/


#outer-box{
    padding-top:5px;
    height:400px;
    width:300px;
    margin:5vh auto;
    background-color:var(--calc-background);
    min-height:400px;
    min-width:30px;
    box-shadow: 6px 6px 1px 0.5px #9AA27C ;
    border-radius:10px;
}
/*----------Display----------------*/
#display{
    display:flex;
    flex-direction:column;
    flex-wrap:nowrap;
    justify-content:flex-start;
    align-items:flex-end;
    row-gap:15%;
    margin:5% auto;
    
    left:7%;
    top:7%;
    width:90%;
    height:22%;
    background-color:var(--display-background);
    color:var(--display-numb-color) ;
    font-family:helvetica;
 
    
    
}
#result{
    margin-top:2%;
    margin-right:5%;
    max-width:85%;
    overflow:hidden;
}
#input{
     margin-right:5%;
     font-size:2rem;
     max-width:85%;
     overflow:hidden;
    
}
/*--------------------------------------*/
/*--------------keyboard----------------*/

#keyboard{
    
    display:grid;
    padding:0 0.5rem 0 1rem;
    height:75%;
    grid-template-columns:repeat(6,16.6%);
    grid-template-rows:repeat(4,22%);
    width:90%;
	
}
.number-button{
    background-color:var(--num-button);
    align-self: stretch;
    
    width: 100%;
    font-weight:bold;
    font-size:1.2rem;
    transition: all .1s ease;
}
.func-button{
    background-color:var(--num-button);
    align-self: stretch;
    
    width: 100%;
    font-weight:bold;
    font-size:1.2rem;
    color:var(--func-button);
    transition: all .07s ease;
}
#equal{
    grid-column:5/7;
}

.clicked{
    border:1px solid var(--animation-color);
    transform:scale(1.1);
}

/*--------------------------------------*/




/*--------------media queries-----------*/



@media only screen and ((max-device-width:850px) or (max-width:850px)){
    .outer-box{
	width: 75%;
    }
   
}

